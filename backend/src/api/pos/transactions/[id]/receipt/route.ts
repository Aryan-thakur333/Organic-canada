import type{MedusaRequest,MedusaResponse}from"@medusajs/framework/http";import{POS_MODULE}from"../../../../../modules/pos";import{requirePosContext}from"../../../../../utils/pos/security";import{PosError,posErrorResponse,type PosService}from"../../../../../utils/pos/contracts";export async function GET(req:MedusaRequest,res:MedusaResponse){try{const s=req.scope.resolve(POS_MODULE) as PosService,t=await s.retrievePosTransaction(req.params.id) as Record<string,unknown>;await requirePosContext(req,String(t.register_id));const receipts=await s.listPosReceipts({transaction_id:t.id}) as unknown[];if(!receipts[0])throw new PosError("POS_RECEIPT_NOT_FOUND","Receipt not found",404);return res.json({receipt:receipts[0]})}catch(error){const out=posErrorResponse(error);return res.status(out.status).json(out.body)}}
