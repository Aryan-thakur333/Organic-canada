import type{MedusaRequest,MedusaResponse}from"@medusajs/framework/http";import{POS_MODULE}from"../../../../modules/pos";import type{PosService}from"../../../../utils/pos/contracts";export async function GET(req:MedusaRequest,res:MedusaResponse){const s=req.scope.resolve(POS_MODULE) as PosService;const filters:Record<string,unknown>={};if(req.query.register_id)filters.register_id=req.query.register_id;if(req.query.status)filters.status=req.query.status;const[transactions,count]=await s.listAndCountPosTransactions(filters,{order:{created_at:"DESC"},take:200}) as[unknown[],number];return res.json({transactions,count})}
