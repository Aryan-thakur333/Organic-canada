import { MedusaContainer } from "@medusajs/framework/types"

export default async function ensureErpTestProductLoader({ container }: { container: MedusaContainer }) {
  // Disabled as per critical architecture correction. Seeding is moved to CLI script.
}
