// The public catalog is filtered client-side for storefront visibility and exact
// regional pricing, so every surface must request the same bounded candidate set.
export const STOREFRONT_PRODUCT_CANDIDATE_LIMIT = 200;
export const STOREFRONT_PRODUCT_PAGE_SIZE = 24;
